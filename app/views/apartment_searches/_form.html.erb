<%= form_for(@apartment_search) do |f| %>
  <% if @apartment_search.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apartment_search.errors.count, "error") %> prohibited this apartment_search from being saved:</h2>

      <ul>
      <% @apartment_search.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :min_price %><br>
    <%= f.number_field :min_price %>
  </div>
  <div class="field">
    <%= f.label :max_price %><br>
    <%= f.number_field :max_price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
