<p id="notice"><%= notice %></p>

<p>
  <%= link_to 'Back', apartment_searches_path, class: 'btn btn-default' %>
  <%= link_to 'Edit', edit_apartment_search_path(@apartment_search), class: 'btn btn-default' %>
</p>

<p>
  <strong>Title:</strong>
  <%= @apartment_search.title %>
</p>

<p>
  <strong>Min price:</strong>
  <%= @apartment_search.min_price %>
</p>

<p>
  <strong>Max price:</strong>
  <%= @apartment_search.max_price %>
</p>

<table class="table table-bordered">
  <thead>
    <tr>
      <td></th>
      <th><%= sortable "title" %></th>
      <th><%= sortable "price" %></th>
      <th><%= sortable "posted_at" %></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <%= render @apartments %>
  </tbody>
</table>
